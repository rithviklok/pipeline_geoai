# Property Tax Defaulter Identification Pipeline — Python dependencies
# Requires Python >= 3.10
#
# Install into a clean virtual environment with:
#   pip install -r requirements.txt

pandas>=1.5.0
pyshp>=2.3.0
shapely>=2.0.0
openai>=1.0.0
